# Wires

Wires connect nodes together, defining the path that data takes through the flow. Data travels from the output of one node to the input of the next.

Multiple wires leading to different end states can have unexpected outcomes unless logic defines how processing should happen.

In the example below wires connect all 5 nodes (for a single processing flow) in the Create Object Group set of nodes.

<figure><img src="/files/oVkEwkITRE7sRuS3USnX" alt=""><figcaption></figcaption></figure>
